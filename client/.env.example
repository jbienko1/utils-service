# Docelowy backend dla proxy Vite (npm run dev / npm run preview)
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Opcjonalnie: publiczna domena za reverse proxy (Vite 6 — bez edycji vite.config.ts)
# __VITE_ADDITIONAL_SERVER_ALLOWED_HOSTS=example.allowedhosts.dev
